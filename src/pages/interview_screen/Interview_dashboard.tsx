"use client"; // only if this page uses hooks or browser-only APIs

import React from "react";
import InterviewPrepDashboard from "@/components/InterviewPrepDashboard";

export default function InterviewPrepDashboardPage(): React.ReactElement {
  return <InterviewPrepDashboard />;
}
